#include "main.h"
/**
* _abs - to prints the absolute value of a number
*@i: the targeted number
*Return: will returns 0
*/
int _abs(int i)
{
if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
